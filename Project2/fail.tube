val x    //; is missing

//program should terminate before scanning the following commands

print("This should not be scanned by flex! if you are seeing this right now, then your program did not halt properly");
